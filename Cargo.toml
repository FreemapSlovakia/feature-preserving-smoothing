[package]
name = "feature-preserving-smoothing"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"
clap = { version = "4", features = ["derive"] }
rayon = "1.12.0"
wbgeotiff = "0.1.2"

[profile.release]
lto = "thin"
codegen-units = 1
